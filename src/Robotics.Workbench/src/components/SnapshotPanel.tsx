import {Snapshot} from '../api/types'; import StatusBadge from './StatusBadge'; export default function SnapshotPanel({snapshot,title='Operation state'}:{snapshot?:Snapshot;title?:string}){return <section className="card"><div className="section-title">{title}<small>OPC UA SNAPSHOT</small></div>{snapshot?.sections?.map(s=><div className="snapshot" key={s.name}><h3>{s.name}</h3>{s.values.map(v=><div className="value" key={v.nodeId}><span>{v.label}</span><b>{v.valueText||'—'}</b><StatusBadge value={v.statusCode}/></div>)}</div>)||<div className="muted">Waiting for gateway data.</div>}</section>}
