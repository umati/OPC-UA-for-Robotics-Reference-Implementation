import {LiveMessage} from '../api/types'; import StatusBadge from './StatusBadge'; export default function LiveTimeline({items}:{items:LiveMessage[]}){return <section className="card timeline"><div className="section-title">Live timeline<small>LIVE DATACHANGE STREAM</small></div>{items.length?items.map((m,i)=><div className="event" key={i}><time>{new Date(m.timestampUtc||m.generatedAtUtc||Date.now()).toLocaleTimeString()}</time><b>{m.label||m.type}</b><span>{m.valueText||m.selection||m.reason||''}</span>{m.statusCode&&<StatusBadge value={m.statusCode}/>}<em>{m.discovery||''}</em></div>):<div className="muted">No live messages.</div>}</section>}
