export default function ConnectionPanel({gateway,setGateway,health,opcua,live,onRefresh,onLive}:{gateway:string;setGateway:(s:string)=>void;health:string;opcua:string;live:string;onRefresh:()=>void;onLive:()=>void}){return <div className="connection"><span className="eyebrow">GATEWAY LINK</span><input value={gateway} onChange={e=>setGateway(e.target.value)}/><span className="dot"/> {health} · OPC UA {opcua}<button onClick={onRefresh}>Refresh</button><button onClick={onLive}>{live==='connected'?'Disconnect live':'Connect live'}</button></div>}
